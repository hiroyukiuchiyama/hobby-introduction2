<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing hobby</h1>
    	<%= render 'layouts/errors', obj: @hobby %>
  		<%= render 'form', hobby: @hobby %>
  		<%= link_to 'Show', hobby_path(@hobby), class: "hobby_#{@hobby.id}" %> |
  		<%= link_to 'Back', hobbys_path, class: "back" %>
  </div>
</div>